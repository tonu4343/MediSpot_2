// pay.jp public key — safe to expose client-side (never put the secret key here).
// Replace with your own test/live public key from the pay.jp dashboard.
window.MEDISPOT_PAYJP = {
  publicKey: "YOUR_PAYJP_PUBLIC_KEY"
};
